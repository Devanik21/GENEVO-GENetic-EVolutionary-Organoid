# Datasets

This directory is intended to store datasets used for evolutionary neural architecture benchmarking.

- **ARC**: Place ARC benchmark data under `arc/`.
- **HLE**: Place Humanity's Last Exam data under `hle/`.
